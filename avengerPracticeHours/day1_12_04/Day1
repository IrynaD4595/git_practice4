
1.Type of casting:
  a. Implicit casting
     - done by compiler;
     - smaller to bigger data type;
     - no loss data;

  b. Explicit casting
     - done by developer;
     - bigger to smaller;
     - we can loos data;

2. String methods:
     charAt();
     length();
     replace();
     replaceFirst();
     trim();
     substring();
     lastIndex();
     contains();
     equals();
     equalsIgnoreCase();
     repeat();
     endsWith();
     startsWith();
     isEmpty();
     isBlank();
     toUpperCase();
     toLowerCase();